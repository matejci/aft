<!DOCTYPE html>
<html>
  <head>
    <meta http-equiv='Content-Type' content='text/html; charset=utf-8' />
    <%= stylesheet_link_tag 'mailer' %>
    <%= yield :header %>
  </head>

  <body>
    <%= yield %>

    <!-- this ensures Gmail doesn't trim the email -->
    <span style="opacity: 0"><%= Time.current %></span>
  </body>
</html>
